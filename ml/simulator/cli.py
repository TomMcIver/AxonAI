"""Command-line entry point for the simulator.

Subcommands (implemented in PR 9):
    calibrate  — run fit_2pl, fit_bkt, priors end-to-end
    run        — python -m ml.simulator run --config configs/small.yaml
    validate   — python -m ml.simulator validate --run-id <id>
"""
