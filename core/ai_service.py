"""Legacy AIService stub — full implementation removed; simulator v2 rebuild pending."""


class AIService:
    """Minimal stub so Flask routes keep importing; chat falls back to demo responses."""

    def __init__(self):
        pass

    def generate_response(self, message, user_id, class_id):
        raise NotImplementedError(
            "Replaced by simulator v2 — see Claude Code rebuild"
        )
