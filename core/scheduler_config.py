"""Scheduler stub — legacy Big AI Coordinator periodic jobs removed; simulator v2 rebuild pending."""


def init_scheduler(app):
    """No-op: background analysis scheduler disabled."""
    return None
