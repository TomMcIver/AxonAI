You are my senior engineer on the AxonAI demo repo.

Goal right now: DO NOT add new features yet. First, get a full picture of what exists, what is working, what is duplicated, and what can be deleted safely so we can rebuild a clean demo architecture that can “slot in” our models later.

Context (what we already have conceptually):
- A “class-locked + student-locked” GPT tutor: it must ONLY talk about that class and that student.
- A Knowledge Tracing (KT) model notebook exists (KT1 / EdNet KT pipeline). It may not be integrated into the app yet.

TASKS (in order):
1) Inventory the codebase:
   - List all folders/files and classify them: frontend, backend, data, notebooks, scripts, configs, tests.
   - Identify how the app runs (entrypoints, commands, env vars).
   - Identify the current API routes and what they do.
   - Identify the DB layer (if any): schema/models/migrations.
   - Identify any model-related code: prompts, model calls, inference code, pipelines.

2) Find what is actually used:
   - Determine which files are imported/executed in the main run path.
   - Identify dead/unused files, duplicate implementations, abandoned experiments.
   - Identify any secrets, keys, or unsafe patterns (hardcoded tokens).

3) Produce a “Delete & Clean Plan” (do NOT execute yet):
   - Propose exactly what files/folders can be deleted with reasons.
   - Propose what should be archived instead of deleted (e.g., notebooks).
   - Propose what should be renamed/reorganized for clarity.
   - Provide a risk level for each deletion (low/medium/high) and how to verify.

4) Produce a clean target architecture that supports plugging models in later:
   - Define a minimal folder structure (e.g., backend/, frontend/, shared/, scripts/).
   - Define strict interfaces for “Model Providers” so we can slot in:
       a) TutorModel (GPT-based)
       b) ProfileSummarizer (can be GPT prompt-based for demo)
       c) QuizGenerator (GPT prompt-based for demo)
       d) KnowledgeTracer (placeholder now; later integrate KT model)
   - Each interface must have: input schema, output schema, and where it lives.

5) Output required deliverables:
   A) A concise repo map (tree + purpose)
   B) A table: File/Folder | Purpose | Used? | Keep/Delete/Archive | Risk
   C) A step-by-step cleanup plan (no code changes yet)
   D) A proposed “minimal demo flow” diagram in text:
      Student login → select class → chat tutor → generate quiz → submit answers → update profile/mastery → simple dashboard

IMPORTANT RULES:
- Do not delete or modify files yet.
- Ask no questions unless absolutely necessary; make reasonable assumptions and clearly mark them.
- Prefer the smallest demo-ready structure, not production complexity.
- Keep the tutor scope enforcement as a non-negotiable requirement.

When finished, stop and wait for my instruction “Proceed with cleanup” before making any edits.
